import 'package:flutter/material.dart';

class SpartanColors {
  static const background = Colors.black;
  static const defaultText = Color(0xFF00FFCC);
  static const success = Colors.greenAccent;
  static const warning = Colors.amberAccent;
  static const error = Colors.redAccent;
  static const glitch = Color(0xFFDA00FF);
  static const inactive = Colors.white38;
  static const neutro = Colors.white;
  static const info = Color.fromARGB(248, 213, 157, 240);
  static const decrypt = Color.fromARGB(250, 96, 111, 240); 
}
